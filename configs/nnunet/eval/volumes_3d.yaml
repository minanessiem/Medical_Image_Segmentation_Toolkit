# @package _global_
#
# nnU-Net evaluation policy for 3D volume predictions. Semantic space is
# supplied by the composed conversion preset through nnunet.export_space.

defaults:
  - /nnunet/eval: default
  - _self_

nnunet_eval:
  input_format: volumes_3d
  levels:
    - slice
    - volume
