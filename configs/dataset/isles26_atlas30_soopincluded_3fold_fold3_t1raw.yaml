# Human fold 3 selects datalist fold value 2.
defaults:
  - isles26_base
  - _self_

fold: 2
partitioning: fold
subsets:
  train:
    fold_not_in: ["${dataset.fold}"]
  val_fast:
    fold_in: ["${dataset.fold}"]
  val_rest:
    fold_in: ["${dataset.fold}"]
  val_full:
    fold_in: ["${dataset.fold}"]
active_subsets:
  train: train
  val: val_full
  sample: val_full
modalities: ['T1_RAW']
num_modalities: 1
nnunet:
  dataset_id: "268"
  dataset_name: "isles26_atlas30_soopincluded_3fold_fold3_t1_raw"
