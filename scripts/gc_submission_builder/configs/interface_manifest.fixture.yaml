# Development fixture only. Replace every slug/path from the official phase interface
# before release certification; socket names deliberately carry no modality semantics.
interfaces:
  - name: fixture-single-nifti
    inputs:
      - slug: opaque-fixture-image
        dataset_key: T1
        relative_path: images/fixture-input
        file_type: nifti
        cardinality: one
    technical_inputs: []
    output:
      slug: opaque-fixture-segmentation
      relative_path: images/fixture-output
      file_type: nifti
